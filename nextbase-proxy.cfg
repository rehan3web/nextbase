global
    maxconn 4096

defaults
    mode tcp
    timeout connect 5s
    timeout client  1m
    timeout server  1m

# Placeholder so HAProxy starts cleanly before any app is deployed.
listen _ready
    bind *:8099
    server noop 127.0.0.1:8099
